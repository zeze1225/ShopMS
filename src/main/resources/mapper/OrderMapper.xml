<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.shoppingcart.mapper.OrderMapper">

    <select id="findByUserId" resultType="Order">
        SELECT * FROM orders WHERE user_id = #{userId} ORDER BY create_time DESC
    </select>

    <select id="searchByUserId" resultType="Order">
        SELECT * FROM orders
        WHERE user_id = #{userId}
        AND (CAST(id AS TEXT) LIKE '%' || #{keyword} || '%'
             OR order_no LIKE '%' || #{keyword} || '%'
             OR status LIKE '%' || #{keyword} || '%')
        ORDER BY create_time DESC
    </select>

    <select id="findById" resultType="Order">
        SELECT * FROM orders WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO orders (user_id, order_no, total, status, address, create_time)
        VALUES (#{userId}, #{orderNo}, #{total}, #{status}, #{address}, datetime('now','localtime'))
    </insert>

    <update id="updateStatus">
        UPDATE orders SET status = #{status} WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM orders WHERE id = #{id}
    </delete>

    <delete id="deleteByIds">
        DELETE FROM orders WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

</mapper>
