<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.shoppingcart.mapper.CartMapper">

    <!-- 关联映射结果集：购物车关联商品名和价格 -->
    <resultMap id="cartResultMap" type="Cart">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="productId" column="product_id"/>
        <result property="quantity" column="quantity"/>
        <result property="productName" column="product_name"/>
        <result property="productPrice" column="product_price"/>
    </resultMap>

    <select id="findByUserId" resultMap="cartResultMap">
        SELECT c.id, c.user_id, c.product_id, c.quantity,
               p.name AS product_name, CAST(p.price AS REAL) AS product_price
        FROM cart c
        LEFT JOIN product p ON c.product_id = p.id
        WHERE c.user_id = #{userId}
        ORDER BY c.id
    </select>

    <select id="findByUserAndProduct" resultType="Cart">
        SELECT * FROM cart WHERE user_id = #{userId} AND product_id = #{productId}
    </select>

    <insert id="insert">
        INSERT INTO cart (user_id, product_id, quantity)
        VALUES (#{userId}, #{productId}, #{quantity})
    </insert>

    <update id="updateQuantity">
        UPDATE cart SET quantity = #{quantity} WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM cart WHERE id = #{id}
    </delete>

    <delete id="deleteByIds">
        DELETE FROM cart WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <delete id="clearByUserId">
        DELETE FROM cart WHERE user_id = #{userId}
    </delete>

</mapper>
